---
- name: Validate SSH configuration
  ansible.builtin.command:
    cmd: /usr/sbin/sshd -t
  changed_when: false
  listen: Reload SSH safely

- name: Reload SSH service
  ansible.builtin.systemd_service:
    name: ssh
    state: reloaded
  listen: Reload SSH safely
