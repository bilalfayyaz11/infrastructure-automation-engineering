---
- name: Validate MySQL configuration
  ansible.builtin.command:
    cmd: mysqld --validate-config
  changed_when: false
  listen: Restart MySQL safely

- name: Restart MySQL
  ansible.builtin.systemd_service:
    name: mysql
    state: restarted
  listen: Restart MySQL safely
