server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;

    root /var/www/automated-webapp;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location = /health {
        access_log off;
        default_type text/plain;
        return 200 "healthy\n";
    }
}
